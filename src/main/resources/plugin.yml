main: ${group}.${pluginName}
name: ${pluginName}
version: ${pluginVersion}
description: ${pluginDescription}
authors:
  - tomwmth
  - BestBearr
website: ${pluginWebsite}
api-version: 1.13

softdepend:
  - Vault
  - ViaVersion